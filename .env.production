# Production Environment Configuration
# Copy this file to .env and customize as needed
# IMPORTANT: Review all settings carefully before deploying to production

# Environment
ENVIRONMENT=production

# API Configuration
# Use 127.0.0.1 if behind reverse proxy (recommended)
API_HOST=127.0.0.1
API_PORT=8000

# File Upload Configuration
MAX_UPLOAD_SIZE_MB=100
MAX_DECOMPRESSED_SIZE_MB=500

# Workspace Configuration
# Use dedicated directory with proper permissions in production
WORKSPACE_BASE_PATH=/var/docc2context/workspaces
WORKSPACE_PREFIX=swift-conv

# Subprocess Configuration
SWIFT_CLI_PATH=/usr/local/bin/docc2context
SUBPROCESS_TIMEOUT=60
MAX_SUBPROCESS_RETRIES=3

# Logging
# Use INFO or WARNING in production (not DEBUG)
LOG_LEVEL=INFO

# CORS (NEVER use * in production!)
# Replace with your actual production domains
CORS_ORIGINS=https://example.com,https://www.example.com

# Security (STRICT in production)
# Replace with your actual production hosts
ALLOWED_HOSTS=example.com,www.example.com,api.example.com
RATE_LIMIT=50

# Application Metadata
APP_NAME=DocC2Context Service
APP_VERSION=0.1.0
APP_DESCRIPTION=Swift DocC to Markdown Web Converter (MVP)

# Production Security Checklist:
# [ ] CORS_ORIGINS does NOT contain *
# [ ] ALLOWED_HOSTS does NOT contain *
# [ ] LOG_LEVEL is INFO or WARNING (not DEBUG)
# [ ] WORKSPACE_BASE_PATH has proper permissions (700)
# [ ] Swift CLI path is absolute and verified
# [ ] Rate limiting is configured appropriately
# [ ] SSL/TLS is enabled (via reverse proxy)
# [ ] Firewall rules are configured
# [ ] Monitoring and alerting are set up
